package controllers;

public class Migrations {
//    CREATE TABLE users (
//            id INT AUTO_INCREMENT PRIMARY KEY,    -- Unique identifier for the user
//            name VARCHAR(255) NOT NULL,           -- User's full name
//    email VARCHAR(255) NOT NULL UNIQUE,   -- User's email address (must be unique)
//    cnic VARCHAR(255) NOT NULL,           -- User's CNIC
//    phone VARCHAR(255) NOT NULL,          -- User's phone number
//    password VARCHAR(255) NOT NULL,       -- User's password (hashed)
//    isRenting BOOLEAN NOT NULL DEFAULT 0, -- Whether the user is renting or not (0 for false, 1 for true)
//    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, -- Timestamp when the user was created
//    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP -- Timestamp when the user was last updated
//);
//
//    CREATE TABLE bicycles (
//            id INT AUTO_INCREMENT PRIMARY KEY,
//            name VARCHAR(255) NOT NULL,
//    year INT NOT NULL,
//    weight FLOAT NOT NULL,
//    current_condition ENUM('Average', 'Good', 'Excellent', 'Perfect') NOT NULL,
//    hourly_rate INT NOT NULL,
//    location VARCHAR(255) NOT NULL,
//    user_id INT NOT NULL,
//    FOREIGN KEY (user_id) REFERENCES users(id)
//            );
}
